name: master pre-merge check

on: 
  push:
    branches:
    - main
  pull_request:
    branches:
    - main

jobs:
  call-test:
    name: call test
    runs-on: ubuntu-latest

    steps:
    - name: pwd
      run: |
        echo "ls"
        pwd
        echo "ls"
        ls
    - uses: actions/checkout@v5
    - name: pwd2
      run: |
        pwd
        echo "ls"
        ls
    - name: wait 60 sec
      run: sleep 60
    - name: test3
      run: echo "call test3"
